{"date":"2026-07-14","score":null,"used":[],"comment":"Config task, no Lua touched — the map tooling was never exercised. Located the commit skill with find+grep over .claude/, which map/ does not index and should not."}
{"date":"2026-07-14","score":3,"used":["map_query","map","grep","read"],"comment":"The load-bearing question was a data-flow one the index cannot express: which paths mutate the notes/ccs arrays directly rather than through the verbs (answer: resolveCollisions and load dedup). map_query has no way to ask what writes a given @state; I found them by grepping `dirty = true` and `notes[...] = nil`. The .map @state lines with their trailing comments were the fastest way to read indexStale semantics, and map/specs/*.map gave me the spec case-lists in one shot."}
{"date":"2026-07-14","score":3,"used":["map_query","grep","read"],"comment":"Mostly went around the maps. The hunt was intra-file — trackerManager file-local closures (mmBatch, idxReconcile, chansInsert, makeEntry) — and .map indexes module surface, not locals, so grep_window was the only tool that could find callers; map_query earned its keep only twice, for mm:modify and mm:poolGuid. The real gap: nothing models lexical scope. The bug was `deferredSort` written at :1515 while its `local` at :608 lives in a do-block that closes near :1200, so the write silently bound a global and the block-local read never saw it. A map that recorded do-block extents — or merely flagged a name referenced outside its declaring block — would have named this in one query, instead of five REAPER instrumentation round-trips."}
{"date":"2026-07-14","score":2,"used":["grep","read"],"comment":"Never opened a map. Two reasons, one mine and one the tooling's. Mine: I knew the files, so I went straight to grep_window/multi_read for exact ranges -- and then guessed tm:channel() twice in a bridge chunk when map/trackerManager.map would have told me it is tm:channels() returning an iterator. The tooling's: the load-bearing question of this session was \"who subscribes to mm's reload signal?\" -- the emits side is indexed (--emits:), the subscribe side is not, and mm:subscribe(name, fn) is a runtime-receiver call so usedby drops it. Had to grep. An index of subscribe(signal) call sites, keyed by signal name, would make the emits edges bidirectional and answer \"what breaks if I change this signal's fire conditions\" in one query."}
{"date":"2026-07-14","score":3,"used":["map_query","map","grep","read"],"comment":"map_query(kind='usedby', module='eventMeta') silently returned eventMeta's OUTBOUND edges, not its callers -- the module filter scopes to the map file an entry lives in, so it and usedby are contradictory and the query answers the wrong question rather than erroring. Wasted a call and I nearly believed it. Either reject the combination or make module mean 'callers in this module'. Separately: I greped tests/specs for sweep|purge to find whether the stale-uuid sweep was covered, when kind='case' query='*metadata*' would likely have found 'loser metadata purged' in one shot -- the spec-map case index is there and I didn't reach for it, so that one's on me."}
{"date":"2026-07-15","score":3,"used":["map_query","map","grep"],"comment":"map_query(kind='usedby', query='cm:get') returned a flat 'no matches' — cm:get has ~100 call sites across 20 prod files. Same runtime-receiver gap as last session's module-filter bug, but the failure mode is worse here: for a shared service, 'who calls this' is THE query before a refactor, and a silent confident 'no callers' is the one wrong answer that could get someone hurt. Consider erroring, or degrading to a textual scan, rather than returning empty. Credit where due: reading map/configManager.map directly earned its keep — the `@use call util.assign @ mergedTable:377` edge told me in one line that deleting mergedTable orphans the file's only util.assign use, which I'd otherwise have grepped for."}
{"date":"2026-07-15","score":null,"used":["grep","read"],"comment":"Docs-archive task: moved 3 design docs and repointed 31 inbound `see design/x.md` pointers. Deliberately null, not 3 — the map tooling was never the right instrument here and shouldn't be scored for it. Worth noting as a gap all the same: .map indexes symbols, not doc pointers, so 'what cites design/dirty-channels.md' is grep-only, and there's no way to ask 'which doc references now dangle'. My ad-hoc check (extract every design/*.md ref, test -f each) found 11 PRE-EXISTING broken links from earlier archive moves that nobody caught. A link-integrity check over the docs graph would have caught those on the commit that broke them; if the map generator ever indexes `see docs/...`/`design/...` pointers, that check comes free."}
{"date":"2026-07-15","score":4,"used":["map_query","map","grep_window","read"],"comment":"map_query module dump gave the whole tm fn/api index in one call — ideal for locating the design doc's line refs (:2215, :2332, etc). grep_window found the 7 note-column insertion sites cleanly. No defect; usedby not needed this pass."}
{"date":"2026-07-15","score":4,"used":["map_query","map","grep_window","read","reaper_eval"],"comment":"map_query fn/api dump kept me oriented across a wide tm change. Gap: no way to ask the map 'how many times per rebuild is this column sorted / which stages call sortByPPQL' — a call-count-in-pipeline view would have surfaced the 4x-redundant-sort finding without hand-tracing the pipeline order."}
{"date":"2026-07-15","score":3,"used":["grep_window","read","reaper_eval"],"comment":"Intra-file dedup (drop one of four note-column sorts). Knew the file and the symbol, so grep_window over trackerManager.lua found the 4 sortByPPQL sites + pipeline order in one pass; Read confirmed rebuildFx sits immediately after computeFxWindows. Didn't reach for map_query — correct for single-file navigation, wouldn't have added anything. Reinforces the prior line's gap: the map can't answer 'how many times per rebuild is a column sorted / which stages call sortByPPQL', the exact fact this change turned on; I hand-traced the pipeline to prove only rebuildFx's sort was redundant."}
{"date":"2026-07-15","score":3,"used":["grep_window","read","reaper_eval"],"comment":"Deep intra-file audit: 'which functions read note columns in ppqL order between two pipeline points, and are they host/window-gated'. grep_window over trackerManager.lua enumerated the readers (eachWindowNote/membersOf/channelStreams/forEachEvent/rebuildRegionPark) and Read classified each. Real gap the map can't close: this is a data-flow + pipeline-ordering question. usedby gives callers but not 'reads structure X in array order', and nothing expresses 'runs between stage A and stage B of rebuildPipeline'. I hand-traced pipeline order from the source. A 'reads-in-order' edge or a pipeline-sequence view would have turned a 6-read audit into one query."}
{"date":"2026-07-15","score":3,"used":["grep","read"],"comment":"live bridge-authoring task needed exact shapes not locations — generators.kinds defaults, addEvent per-evType accepted fields (cc/pa/at), markGroup rect — so went straight to source via grep_window. A map_query surfacing a module data-table of defaults (generators.kinds) would have saved 3-4 reads."}
{"date":"2026-07-15","score":4,"used":["map_query","grep","read"],"comment":"map_query resolved the voicing shape-peer cleanly in one call; rest of the session was new-file authoring (intervals.lua) plus a fixture edit, little to index against"}
{"date":"2026-07-15","score":4,"used":["map_query","grep","read"],"comment":"map_query cleanly located the low-level verbs (addLowlevel/assignLowlevel/deleteLowlevel) and the intervals API in one call each; no gaps this session. Source-range reads still went through multiread/grep_window, as expected."}
{"date":"2026-07-16","score":4,"used":["map_query","grep","read"],"comment":"map_query rebuild on tm was the whole orientation pass - stage fns with ranges plus contracts/invariants in one call; niggle: annotation hits print file-only, no line to jump to"}
{"date":"2026-07-16","score":3,"used":["map_query","grep","read"],"comment":"core task was finding every ds:get(key) read inside the pipeline stages — a text sweep grep_window handled well; map_query gave the stage fn ranges but cannot express dataStore-key reads as a queryable edge, so the actual survey was all grep"}
{"date":"2026-07-16","score":3,"used":["map","grep","read"],"comment":"Meta-session: the map tooling WAS the task. Reading trackerManager.map directly cracked it — the `@use call cm.get` line showed chunk callers stored cm:get under the unresolved dep-name while patternEditor (construct) stored configManager.get, so a module's callers split across two keys and usedby never unified them. Second find: usedby was never a reverse index — it ran the SAME loop as uses, so usedby+module returned the module's OWN outbound edges (the back-to-front the feedback log flagged twice). Fixed both: generator now stores receivers source-faithfully (cm:get, colon kept, no cross-file resolution — keeps per-file regen pure), and the query resolves short names via the self= registry, accepts all four spellings, and usedby is a true reverse scan with module= naming the used target. map_query couldn't be exercised live (running server holds old code); verified new logic by importing the module against real maps."}
{"date":"2026-07-16","score":3,"used":["map","grep","read"],"comment":"Closed feedback line 15's own complaint: annotation hits (invariant/contract/shape/reaper) now emit file:line, so you can jump to them. As an instrument this session, grep over map_extract.py found the emit paths (fmt_ann / attach_annotations / shape block) faster than any map query could — the generator's own structure is not indexed by the maps it produces, which is correct and fine. Reading trackerManager.map directly confirmed the new `@ <line>` suffix and that header annotations (@deps) stay lineless. The line was already carried to pending_annotations and dropped only at attach; shape_annotations already kept a 4-tuple, so the fix was widening every annotation to match. No new gap surfaced."}
{"date":"2026-07-16","score":4,"used":["map_query","grep","read"],"comment":"map_query gave the rebuild* stage roster with line numbers in one call — clean entry point. The per-field producer/consumer split of the fx bag came from grep_window over fx.<field>, not the index; that's field-level dataflow, below map granularity, so expected rather than a gap."}
{"date":"2026-07-16","score":4,"used":["map_query","grep","read"],"comment":"map_query strong for production nav (annotations, api, mmBatch). Two edges: a |-alternation query on kind=api matched nothing (it's glob not regex, so I re-ran with * and filtered); and the test harness (harness.lua/fakeReaper.lua) isn't in the index, so every harness-surface question fell back to grep/read."}
{"date":"2026-07-16","score":4,"used":["map","grep"],"comment":"map/bridge.map located render/parseDirectives/DEFAULTS with line ranges in one read; grepped bridge.lua only to confirm the truncation call sites. Didn't need map_query — single-file change."}
{"date":"2026-07-16","score":4,"used":["map_query","grep","read"],"comment":"fn-range queries anchored every read precisely; usedby on a file-local fn (findNoteColumnForPitch) returns nothing, so local call sites still need a grep fallback"}
